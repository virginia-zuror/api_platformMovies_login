/* const request = require('supertest')
const server = require('../index');

describe('POST api/v1/platforms', ()=>{
    test('shold store a new platform', async()=>{
        await request(server).post('/api/v1/platforms').set('Accept', 'application/json').expect('Content-Type', /json/).expect(201)
    })
})

 */